# Spring Boot Debugging

Entry point and REST endpoint for Spring-based debugging:

- `DebugJavaAppsApplication.java`: Main Spring Boot app.
- `DebugController.java`: Endpoint with division logic for live debugging.
- `DemoApplication.java`: Optional second starter (can be removed).

👉 Run the app and test with: `http://localhost:8080/debug?value=7`
