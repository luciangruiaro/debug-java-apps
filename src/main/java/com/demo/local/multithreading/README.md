# Multithreading & Race Conditions

Demonstrates a race condition using threads:

- `MultiThreading.java`: Two threads adding to a shared list.

👉 Use thread view in the debugger, breakpoints with thread filters, and watch list size.
